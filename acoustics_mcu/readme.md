# Teensy 4.1 firmware

This directory contains

- Teensyduino
This directory contains the unchanged code that team 2024 created
For now the only code that actually works

- teensy41
This directory contains acoustics mcu code that uses the mcuxpresso sdk
instead of arduino. As of now this code is not tested and does not work
